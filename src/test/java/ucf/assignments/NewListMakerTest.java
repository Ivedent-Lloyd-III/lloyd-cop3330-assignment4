package ucf.assignments;

import static org.junit.jupiter.api.Assertions.*;

class NewListMakerTest {
    // opens the file
    // adds new data to the file
    // reads the new file and makes sure data is inside the file
    // close the file
}